import { changeQuestionStatus, createQuestion, publishQuestionBank } from "../actions";
import { createAdminClient } from "@/lib/supabase/admin";

export default async function AdminQuestionsPage({ searchParams }: { searchParams: Promise<{ error?: string; notice?: string }> }) {
  const params = await searchParams; const admin = createAdminClient();
  const [{ data: banks }, { data: questions }] = admin ? await Promise.all([
    admin.from("question_banks").select("id,course_code,course_title,status").order("course_code"),
    admin.from("questions").select("id,topic,status,sample,created_at,question_banks(course_code),question_versions(prompt)").order("created_at", { ascending: false }).limit(30),
  ]) : [{ data: [] }, { data: [] }];
  return <><header className="platform-heading"><div><span className="eyebrow">Academic review workflow</span><h1>Question banks</h1><p>Draft original questions, submit them for review, and publish a bank only after it has 100 approved questions including 15 free samples.</p></div></header>{params.error && <p className="form-message form-message-error">{params.error}</p>}{params.notice && <p className="form-message form-message-success">{params.notice}</p>}<section className="platform-panel"><h2>Create draft question</h2><form action={createQuestion} className="platform-form"><div className="platform-form-grid"><label>Course bank<select name="courseCode" required>{banks?.map((bank) => <option key={bank.id} value={bank.course_code}>{bank.course_code} — {bank.course_title}</option>)}</select></label><label>Topic<input name="topic" required /></label><label>Learning objective<input name="learningObjective" required /></label><label>Difficulty<select name="difficulty" defaultValue="1"><option value="1">Foundational</option><option value="2">Applied</option><option value="3">Challenging</option></select></label><label>Source unit<input name="sourceUnit" required /></label><label>Source page<input name="sourcePage" /></label></div><label>Original question prompt<textarea name="prompt" rows={4} minLength={12} required /></label><div className="platform-form-grid">{["A","B","C","D"].map((label) => <label key={label}>Option {label}<input name={`option${label}`} required /></label>)}</div><div className="platform-form-grid"><label>Correct option<select name="correctLabel" defaultValue="A">{["A","B","C","D"].map((label) => <option key={label}>{label}</option>)}</select></label><label className="platform-check"><input type="checkbox" name="sample" />Free diagnostic sample</label></div><label>Explanation<textarea name="explanation" rows={5} minLength={20} required /></label><button className="button" type="submit">Create draft</button></form></section><section className="platform-panel"><h2>Bank release gates</h2><div className="platform-ticket-list">{banks?.map((bank) => <article key={bank.id}><div><strong>{bank.course_code} — {bank.course_title}</strong><span>{bank.status}</span></div><form action={publishQuestionBank}><input type="hidden" name="bankId" value={bank.id} /><button type="submit">Publish after gate check</button></form></article>)}</div></section><section className="platform-panel"><h2>Recent questions</h2><div className="platform-ticket-list">{questions?.map((question) => { const versions = question.question_versions as unknown as Array<{ prompt: string }>; const bank = question.question_banks as unknown as { course_code: string } | null; return <article key={question.id}><div><strong>{bank?.course_code ?? "Course"}: {versions?.[0]?.prompt ?? question.topic}</strong><span>{question.status}{question.sample ? " · sample" : ""}</span></div><div className="admin-inline-actions">{question.status === "draft" && <form action={changeQuestionStatus}><input type="hidden" name="questionId" value={question.id} /><input type="hidden" name="status" value="review" /><button>Submit for review</button></form>}{question.status === "review" && <form action={changeQuestionStatus}><input type="hidden" name="questionId" value={question.id} /><input type="hidden" name="status" value="published" /><button>Approve</button></form>}{question.status === "published" && <form action={changeQuestionStatus}><input type="hidden" name="questionId" value={question.id} /><input type="hidden" name="status" value="retired" /><button>Retire</button></form>}</div></article>; })}</div></section></>;
}
