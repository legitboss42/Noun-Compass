import { importTimetable, publishSchedule } from "../actions";
import { createAdminClient } from "@/lib/supabase/admin";

export default async function AdminSchedulesPage({ searchParams }: { searchParams: Promise<{ error?: string; notice?: string }> }) {
  const params = await searchParams; const admin = createAdminClient();
  const { data: versions } = await admin?.from("exam_schedule_versions").select("id,label,exam_mode,status,source_url,created_at,academic_terms(name,session_code)").order("created_at", { ascending: false }).limit(20) ?? { data: [] };
  return <><header className="platform-heading"><div><span className="eyebrow">Versioned official data</span><h1>Exam schedules</h1><p>Import a structured copy of an official timetable, review conflicts outside the public site, then publish the approved version.</p></div></header>{params.error && <p className="form-message form-message-error">{params.error}</p>}{params.notice && <p className="form-message form-message-success">{params.notice}</p>}<section className="platform-panel"><h2>Import draft CSV</h2><form action={importTimetable} className="platform-form"><div className="platform-form-grid"><label>Term name<input name="termName" placeholder="2026_2 academic term" required /></label><label>Session code<input name="sessionCode" placeholder="2026_2" required /></label><label>Version label<input name="label" placeholder="Final POP timetable" required /></label><label>Exam mode<select name="examMode"><option value="e-exam">E-exam</option><option value="pop">POP</option><option value="mixed">Mixed</option></select></label></div><label>Official source URL<input name="sourceUrl" type="url" required /></label><label>CSV content<textarea name="csv" rows={12} required placeholder={'course_code,course_title,exam_date,starts_at,venue\nGST101,Use of English,2026-10-01,08:30,Lagos'} /></label><button className="button" type="submit">Validate and import draft</button></form></section><section className="platform-panel"><h2>Schedule versions</h2><div className="platform-ticket-list">{versions?.map((version) => { const term = version.academic_terms as unknown as { name: string; session_code: string } | null; return <article key={version.id}><div><strong>{version.label}</strong><span>{version.status}</span></div><small>{term?.session_code} · {version.exam_mode} · <a href={version.source_url}>source</a></small>{version.status === "draft" && <form action={publishSchedule}><input type="hidden" name="versionId" value={version.id} /><button>Publish reviewed schedule</button></form>}</article>; })}</div></section></>;
}
